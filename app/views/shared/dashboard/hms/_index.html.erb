<div class="bg-white text-gray-700 font-semibold text-center shadow sm:rounded-lg">
  <div class="py-4 mt-6 grid grid-cols-1 gap-5 md:grid-cols-2">
    <div class="flex flex-col items-center">
      <h3 class="mb-2"><%= hms[:title] %></h3>
      <div>
        <canvas id="<%= hms[:id] %>" data-controller="gauge" data-gauge-info-value="<%= hms.to_json %>"></canvas>
      </div>
    </div>
    <div class="flex flex-col items-center">
      <h3 class="mb-2"><%= involvement[:title] %></h3>
      <div>
        <canvas id="<%= involvement[:id] %>" data-controller="gauge" data-gauge-info-value="<%= involvement.to_json %>"></canvas>
      </div>
    </div>
  </div>
</div>
