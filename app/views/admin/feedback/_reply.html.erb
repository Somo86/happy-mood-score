<div class="mt-2 mr-2 px-3 py-1 text-sm text-gray-600 bg-gray-50 rounded">
  <p class="mt-1 font-semibold">
  <%=  t 'feedbackItem.replyBy', name:  reply.employee.name, date: l(reply.created_at, format: :short)  %>
  </p>
  <p class="mt-1">
  <%=  reply.description  %>
  </p>
</div>
