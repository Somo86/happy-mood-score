<% order = params[:order].to_s == 'asc' ? 'desc' : 'asc' %>
<% order_link =  teams_admin_ranking_index_url(order: order) %>
<%= render 'header', title: t('dashboardTeams.teamsRank'), order_link: order_link, order_title: @table_header[:order_link] %>
<%= render 'table' %>
